import styled from 'styled-components';

const CostChartStyles = styled.div`
    width: calc(75% - var(--gap));
    height: calc(50% - var(--gap)/2);
    margin-left: var(--gap);
`;

export default CostChartStyles;
